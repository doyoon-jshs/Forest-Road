# REPORT_NOTES.md — 보고서/포스터 작성용 자료

`CONTEXT.md`가 "다른 개발자/LLM이 코드를 이어받기 위한" 기술 문서라면, 이 파일은 **보고서나 학술
포스터를 쓸 때 그대로 가져다 쓸 수 있는 소재**를 정리한 것이다. 배경·목적·방법론·실험결과·한계처럼
보고서 목차 순서에 맞춰 구성했다.

## 1. 프로젝트 기본 정보

- **제목(후보)**: 지형 기반 임도 노선 최적화 / DEM 기반 산림 도로 자동 설계 시스템
- **한 줄 요약**: DEM(수치표고모델)만 입력하면 경사·계곡 횡단·곡선반경·절성토량을 종합적으로
  고려해 임도(산림 도로) 노선을 자동으로 탐색·비교하는 웹 기반 도구
- **테스트 지역**: 제주도 (Copernicus DEM, `sample_data/jeju_copernicus_dem.tif`)
- **키워드**: DEM, GIS, 임도 설계, 경로 최적화, 최소비용경로(least-cost path), D8 흐름누적,
  절/성토(cut/fill), 지형분석, 산림공학

## 2. 배경 및 필요성 (보고서 서론용)

- 임도는 산림 경영(간벌·수확·조림·산불 진화 등)의 접근성을 좌우하는 핵심 인프라지만, 노선 설계는
  전통적으로 현장 답사와 설계자의 경험에 크게 의존해왔다.
- 수작업 설계는 (1) 넓은 후보 지역을 전부 답사하기 어렵고, (2) 여러 대안 노선을 정량적으로 비교하기
  어려우며, (3) 절/성토 물량 같은 공사비 관련 지표를 설계 초기 단계에서 정량적으로 파악하기 어렵다는
  한계가 있다.
- 반면 위성·항공 기반 DEM(Copernicus GLO-30 등)은 무료로 넓은 지역을 커버하므로, 이를 활용하면 설계
  초기 단계에서 여러 후보 노선을 자동으로 생성·비교하는 것이 가능하다.
- 특히 제주도처럼 화산지형(현무암질)이고 계곡(건천 포함)이 많은 지역은 경사와 계곡 횡단이 노선 설계의
  핵심 제약이 되므로, 이를 정량적으로 모델링하는 것이 실질적으로 의미가 크다.
- 임도는 산림 경영뿐 아니라 **교통(이동) 인프라**로서의 성격도 함께 가진다.
  - **농산촌 교통망과의 연계**: 산간 마을은 임도가 사실상 마을 간 유일한 이동·물류 경로인 경우가
    많아, 임도망이 곧 농산촌 지역의 교통 접근성을 좌우한다.
  - **간선 교통로로의 확장 사례**: 제주도의 5·16도로, 1100도로처럼 원래 산악·산림 지역을 관통할
    목적으로 개설된 도로가 이후 지역 간(제주시↔서귀포시) 이동을 위한 주요 간선 교통로로 정착한
    사례가 있다. 즉 산림·산악 도로 설계는 처음부터 일반 교통 수요까지 염두에 둘 필요가 있다.
  - **주행 안전(교통안전) 기준으로서의 곡선반경**: 이 프로젝트가 노선 스무딩 후 계산하는
    최소곡선반경(15m 기준)은 산림 작업 편의만이 아니라, 차량이 안전하게 주행할 수 있는 최소 회전
    반경이라는 교통안전 설계기준의 성격도 가진다.
  - **재해 대응 이동로**: 산불 진화 외에도 산사태 등 산악 지역 재해 발생 시 구조·복구 장비와 인력이
    현장까지 접근하는 유일한 이동로가 임도인 경우가 많아, 재해 대응 관점의 교통 인프라로도 기능한다.

## 3. 목적 및 목표

- DEM으로부터 지형 제약(경사, 계곡 횡단, 곡선반경)을 반영한 비용함수를 정의하고, 이를 바탕으로
  최소비용경로 알고리즘으로 임도 노선을 자동 생성한다.
- 생성된 노선에 대해 절/성토 물량을 추정해 공사 규모를 초기 단계에서 정량적으로 제시한다.
- 여러 대안 노선을 생성·비교해 설계자가 의사결정할 수 있는 정보를 제공한다.

## 4. 독자를 위한 배경 개념 설명 (보고서 "이론적 배경" 섹션용)

포스터/보고서 독자가 GIS 비전공자일 수 있다는 가정하에 짧게 풀어쓴 설명.

- **DEM(Digital Elevation Model)**: 지표면 고도를 격자(grid) 형태로 저장한 데이터. 이 프로젝트에서는
  Copernicus GLO-30(전 지구 30m 해상도 위성 기반 DEM)을 사용.
- **D8 흐름누적(flow accumulation)**: 각 격자 셀에서 물이 가장 가파른 이웃 방향(8방향 중 하나)으로
  흐른다고 가정하고, 고도가 높은 곳부터 낮은 곳 순서로 상류 셀 개수를 하류에 누적시키는 알고리즘.
  누적값이 클수록 "더 많은 상류 지역의 물이 모이는 지점" = 하천/계곡일 가능성이 큰 지점으로 해석한다.
  지형학·수문학에서 유역면적을 근사할 때 널리 쓰이는 표준 기법이다.
- **최소비용경로(least-cost path)**: 격자의 각 셀에 "이동 비용"을 매기고, 시작점에서 도착점까지 누적
  비용이 최소가 되는 경로를 찾는 문제. 다익스트라 알고리즘 계열로 풀리며, 이 프로젝트는
  `scikit-image`의 `MCP_Geometric` 구현을 사용한다.
- **절토/성토(cut/fill)**: 도로 설계고(설계 노면)가 원래 지형보다 낮으면 땅을 깎아야 하고(절토), 높으면
  흙을 쌓아야 한다(성토). 이 물량이 곧 공사비와 직결되는 핵심 지표다.
- **양단면적평균법(end-area method)**: 일정 간격으로 자른 단면적의 평균에 구간 길이를 곱해 토공량을
  누적 계산하는 표준 토공량 산정법.

## 5. 시스템 구조 (다이어그램으로 옮기기 좋은 형태)

```
[사용자] --DEM 업로드(GeoTIFF)--> [FastAPI 백엔드]
                                      |
                                      | 1) 좌표계 통일(UTM 52N 재투영)
                                      | 2) 시각화 PNG 생성(음영기복/경사도/고도, WGS84 재투영)
                                      v
                              [React+Leaflet 지도] <-- 사용자가 지도 클릭(시작/경유/도착점)
                                      |
                                      v
                          POST /route {waypoints, road_width_m, seed}
                                      |
                                      v
                        [비용표면 생성] -> [최소비용경로 탐색] -> [경로 스무딩]
                        (경사+계곡횡단 페널티)   (MCP_Geometric)   (Douglas-Peucker+Chaikin)
                                      |
                                      v
                          [절/성토 물량 산출] (교량 구간 제외)
                                      |
                                      v
                    노선 좌표·종단면도·절성토량·교량정보 반환 --> [지도+종단면도 시각화, 비교/내보내기]
```

풀 파이프라인의 함수/근거는 `CONTEXT.md`의 "핵심 개념 2" 참고.

## 6. 방법론 상세 — 보고서 "연구 방법" 절에 그대로 쓸 수 있는 수식/기준

**비용함수**

```
cost(cell) = 1 + grade_penalty(slope) + crossing_penalty(accumulation, concavity)

grade_penalty = (slope / 14%)^2                      if slope <= 14%
              = 1 + 20 * (slope - 14%)                if slope > 14%   (급격히 증가)
cost = ∞  if slope > 30%  또는  바다(고도 <= 0)
```

- 설계기준 종단기울기 14%, 절대 통행 불가 기준 30% (임도 설계기준 근사치로 상수화, `routing.py`
  `MAX_GRADE_PERCENT`/`ABSOLUTE_MAX_GRADE_PERCENT`)

**계곡 횡단 판정 (교량/암거 분류)**

- 유역면적(D8 누적값)이 DEM 내 상위 5%(culvert) / 상위 0.5%(bridge) 백분위수를 넘고, 동시에 그
  지점이 실제로 오목한(주변보다 낮은) 지형일 때만 "하천 횡단"으로 인정 → D8이 평탄 지형에서 만드는
  계산 아티팩트를 걸러내기 위함.
- 상대적 순위 기반 근사치이며, 실제 설계에는 합리식(Q = C·I·A, 유출계수·강우강도·유역면적) 기반
  정밀 수리계산이 필요하다는 점을 명시.

**경로 스무딩**

- 격자 탐색 결과(8방향 이동)는 지그재그 형태 → Douglas-Peucker 알고리즘으로 직선에 가까운 중간점을
  제거(허용오차 1.5셀) → Chaikin 코너커팅(3회 반복, 매 반복마다 각 구간을 25%/75% 지점 두 점으로
  대체)으로 모서리를 둥글림 → 3점 외접원 공식으로 곡선반경 계산, 최소곡선반경 15m 기준 미달 구간 검출.

**절/성토 산출**

- 10m 간격으로 원본 해상도 DEM을 재샘플링, 설계 노면(station 간 직선 보간)과 실제 지형고 차이로
  절토/성토 깊이 산출 → 사다리꼴 단면적(도로폭×깊이 + 사면기울기계수×깊이², 절토 1:1/성토 1:1.5) →
  양단면적평균법으로 물량 누적. 교량으로 분류된 구간은 성토 계산에서 제외하고 교량 길이로 별도 집계.

**대안 노선 생성**

- 결정론적 탐색이 항상 같은 노선을 주는 문제를 해결하기 위해, 시드 기반 난수(균등분포 ±15%)를 비용
  표면의 통행 가능한 셀에만 곱해 재탐색. 같은 시드 → 같은 노선(재현성 보장).

## 7. 주요 기능 / 차별점 (포스터 bullet용 — 짧고 임팩트 있게)

- DEM 업로드 한 번으로 음영기복·경사도·고도 히트맵·등고선 자동 시각화
- 지도 클릭만으로 다지점 경유 노선 자동 탐색 (경사·계곡·통행불가 지역 자동 회피)
- D8 알고리즘 특유의 평탄지형 오판 문제를 오목도(concavity) 검증으로 자체 보정
- 계곡 규모에 따라 암거/교량을 자동 구분하고, 교량 구간은 절/성토 계산에서 합리적으로 제외
- 절/성토 물량을 종단면도와 함께 즉시 산출 — 설계 초기 단계 공사비 추정 가능
- 시드 기반 대안 노선 생성으로 동일 구간에 대해 여러 후보를 비교 가능
- 여러 노선을 동시에 저장해 길이·경사·절성토·교량 길이를 표로 비교, GeoJSON/CSV로 내보내기

## 8. 실험/검증 결과 (보고서 "실험 및 결과" 절 — 개발 중 실제로 확인한 수치)

이 프로젝트는 정식 논문 수준의 정량 검증(현장 측량 대조 등)은 하지 않았고, 아래는 **알고리즘이
의도대로 동작하는지 자체 검증한 기록**이다. 보고서에 "한계"와 함께 명시하는 것을 권장한다.

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 좌표계 재투영 정확성 | UTM 격자를 WGS84로 재투영한 이미지의 종횡비를 실제 bounds 종횡비와 비교 | 재투영 전 0.845(왜곡) → 재투영 후 1.017(bounds와 일치), 지도 라벨과 육안 정합 확인 |
| 바다 마스킹 | 바다 위 지점 클릭 | "통행 불가 지역" 오류로 정확히 거부됨 |
| D8 아티팩트 필터링(오목도 게이팅) | 대칭 지형에서 오목도 필터 적용 전/후 비교 | 교량/암거 등급 셀 중 각각 약 9%/11%가 비(非)오목 아티팩트로 확인되어 제거됨. 실제 노선 하나 기준 계곡 횡단 수가 14곳(전부 암거) → 8곳(암거 7 + 진짜 대형 계곡 교량 1, 상대 유역면적 13배)으로 보정 |
| 교량 구간 성토 제외 | 60m 깊이 합성(synthetic) 계곡에서 교량 처리 유무 비교 | 미처리 시 169,200㎥의 비현실적 성토량 산출 → 교량 처리 후 성토 0㎥ + 교량 35m로 보정 |
| 시드 재현성 | 동일 시드로 반복 요청 | 항상 동일한 노선 반환(결정론적) 확인 |
| 처리 성능 | 제주 전체 DEM(다운샘플 후 셀 약 57m, `MAX_ROUTING_DIM=2000`) 기준 경로 탐색 | 약 2초 이내 |

## 9. 스크린샷/그림으로 넣으면 좋은 장면 (포스터 시각 자료 제안)

- DEM 업로드 후 음영기복/경사도/고도 3종 오버레이 비교 스크린샷
- 지도 위에 그려진 노선 + 계곡 횡단 지점(암거/교량) 표시 (교량은 보라색 굵은 선)
- 종단면도(절토=빨강, 성토=파랑 음영, 교량 구간 보라색 선) 스크린샷
- 같은 구간에 대해 시드를 바꿔 생성한 2~3개 대안 노선을 지도 위에 겹쳐 그린 비교 스크린샷
- 여러 노선 비교 테이블(길이/경사/절토/성토/교량) 스크린샷
- 위 "6. 방법론" 파이프라인 다이어그램(직접 재작성 권장)

## 10. 한계 (보고서에 정직하게 명시할 것)

- 도로 횡단(폭 방향) 지형 경사 미반영 — 종단(중심선 방향)만 고려하는 단순화 모델.
- 계곡 횡단 구조물 분류가 실제 유량 계산이 아닌 DEM 내 상대적 유역면적 순위 기반 근사치.
- D8은 sink-fill 전처리가 없고 단일 흐름방향만 가정하는 단순화된 수문 모델.
- 제주도처럼 화산암(현무암) 지형에서는 지형상 배수 분지가 커도 실제로는 상시 유수가 없는 건천인
  경우가 많아, 유역면적 기반 판단과 실제 유출량 사이 괴리가 다른 지역보다 클 수 있음.
- 강우량·토양·식생 등 실제 수문 인자를 전혀 반영하지 않음.
- 보전구역·문화재구역 등 법적/제도적 제약 조건 미반영.
- 절/성토 산출도 표준 사면기울기 가정치를 사용한 근사치이며 현장 지질조사를 대체할 수 없음.

## 11. 향후 연구 방향 (보고서 "결론 및 향후 과제")

- 도로 횡단 방향 지형을 반영한 3차원 단면 기반 절/성토 정밀화
- 강우-유출 모델(합리식 등)을 결합한 계곡 횡단 구조물 판정 고도화, 지역별(예: 제주 현무암 지대)
  유출계수 보정
- 보전구역·문화재구역 등 GIS 레이어를 제약 조건으로 결합
- 다수 후보 노선에 대한 다목적 최적화(공사비·환경영향·접근성 동시 고려)

## 12. 데이터 출처 및 인용 (보고서에 반드시 명시)

- 테스트에 사용한 DEM은 Copernicus DEM(GLO-30, 전 지구 30m 해상도, ESA/Copernicus 제공)이다.
- **주의**: 실제 보고서/포스터에 인용 문구를 넣을 때는 DEM을 내려받은 배포처(예: Copernicus
  Data Space Ecosystem, OpenTopography, AWS Open Data 등)가 요구하는 정확한 출처 표기 문구를
  다운로드 페이지에서 다시 확인해서 그대로 기재할 것 — 배포처마다 요구하는 인용 형식이 조금씩 다르다.
  이 저장소에는 그 출처 URL이 별도로 기록되어 있지 않다.
- 지도 배경 타일: OpenStreetMap contributors (`© OpenStreetMap contributors` 표기 필요).

## 13. 포스터 레이아웃 제안 (전형적인 학술 포스터 구성)

```
+-----------------------------------------------------------+
| 제목 / 저자 / 소속                                          |
+---------------------+---------------------+-----------------+
| 1. 배경 및 필요성     | 4. 실험 및 결과       | 6. 결론/향후연구  |
| (2절 내용 요약)       | (8절 표+스크린샷)     | (11절)           |
+---------------------+---------------------+-----------------+
| 2. 목적              | 5. 스크린샷           |                 |
| (3절)                | (9절 그림들)          |                 |
+---------------------+---------------------+-----------------+
| 3. 방법론            |                       | 참고문헌/데이터출처|
| (5절 다이어그램+6절 핵심 수식만 발췌)                        | (12절)           |
+-----------------------------------------------------------+
```

포스터는 텍스트를 최소화하고 5절 다이어그램 + 8절 표 + 9절 스크린샷 3~4장으로 시각적 비중을 크게
가져가는 걸 추천한다. 보고서는 이 문서의 절 순서(1→13, 12절 제외 가능)를 그대로 목차로 써도 된다.
